<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Nuevo medicamento</title>
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/login_register.css">
    <link rel="stylesheet" href="/css/medication_form.css"> </head>
<body>
{{> header}}

<h1>Nuevo medicamento</h1>

<div class="form-container">
    <form action="/medications/save" method="post" enctype="multipart/form-data">
        <div>
            <label for="name">Nombre:</label>
            <input type="text" id="name" name="name" maxlength="30" required>
        </div>
        <div>
            <label for="stock">Número de pastillas en posesión:</label>
            <input type="number" id="stock" name="stock" step="any" min="0" required>
        </div>
        <div>
            <label for="instructions">Instrucciones de uso:</label>
            <textarea id="instructions" name="instructions" maxlength="100" rows="4" required></textarea>
        </div>
        <div>
            <label for="dose">Dosis de cada pastilla en mg:</label>
            <input type="number" id="dose" name="dose" step="any" min="0" required>
        </div>
        <div>
            <label for="imageFile">Seleccionar imagen:</label>
            <input type="file" id="imageFile" name="imageFile" accept="image/*">
            <p class="file-info-text"><small>Solo se aceptan imágenes (JPG, PNG, GIF).</small></p>
        </div>
        <div class="form-actions">
            <button id="create" type="submit" class="action-button primary-button">Añadir medicamento</button>
            <a href="/medications/" class="action-button secondary-button">Volver</a>
        </div>
    </form>
</div>
</body>
</html>
