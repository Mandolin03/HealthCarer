<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Añadir Nuevo Tratamiento</title>
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/login_register.css">
    <link rel="stylesheet" href="/css/treatment_form.css"> </head>
<body>
{{> header}} <h1>Añadir Nuevo Tratamiento</h1>

<div class="form-container">
    <form action="/treatments/save" method="post">
        <div>
            <label for="name">Nombre del Tratamiento:</label>
            <input type="text" id="name" name="name" maxlength="50" required>
        </div>

        <div>
            <label for="startDate">Fecha de Inicio:</label>
            <input type="datetime-local" id="startDate" name="startDate" required>
        </div>

        <div>
            <label for="endDate">Fecha de Fin:</label>
            <input type="datetime-local" id="endDate" name="endDate" required>
        </div>

        <div>
            <label for="dispensingFrequency">Frecuencia de Dispensación (horas):</label>
            <input type="number" id="dispensingFrequency" name="dispensingFrequency" step="0.1" min="0.1" required>
        </div>

        <div class="form-group checkbox-group">
            <label>Medicamentos a incluir:</label>
            {{^medications}}
                <p class="no-meds-available">No hay medicamentos disponibles para seleccionar. <a href="/medications/new">Añadir uno ahora</a>.</p>
            {{/medications}}
            {{#medications}}
                <div class="checkbox-item">
                    <input type="checkbox" id="medication_{{id}}" name="medicationIds" value="{{id}}">
                    <label for="medication_{{id}}">{{name}}</label>
                </div>
            {{/medications}}
        </div>

        <div class="form-actions">
            <button type="submit" class="action-button primary-button">Añadir Tratamiento</button>
            <a href="/treatments/" class="action-button secondary-button">Cancelar</a>
        </div>
    </form>
</div>
</body>
</html>
